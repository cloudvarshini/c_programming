#include<stdio.h>
int main()
{
    int n;
    char name[30];
    printf("Enter the name:");
    scanf("%s",name);
    printf("Enter the number:");
    scanf("%d",&n);
    if(n%2==0)
    {
        printf("%s\n",name);
        printf("%s\n",name);
    }
    else
    {
        printf("%s\n",name);
    }
}